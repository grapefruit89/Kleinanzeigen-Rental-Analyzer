# archive

Historische Dateien, nicht Teil der geladenen Erweiterung.

- Frueher im Root: `altes userscript.js` (git-Historie behalten).
- `old_versions/` aus `scripts/old_versions/`.

Sideload liest `manifest.json` im Repo-Root. Kein src/-Schnitt ohne Bundler.
